import mockApi from "./api";

export const encodeUrl = mockApi.encode;
export const decodeUrl = mockApi.decode;
export const getUrlStats = mockApi.getStatistics;
export const getUrlList = mockApi.getList;
export const incrementVisit = mockApi.incrementVisit;
